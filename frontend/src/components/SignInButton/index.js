import { Container, Icon, Title } from './styles';

export const SignInButton = ({ title }) => {
  return(
    <Container>
      {/* <Icon></Icon> */}
      <Title>{ title }</Title>    
    </Container>
  )
}